# # database init, supports mysql too
# database=mysql
# spring.datasource.url=${MYSQL_URL:jdbc:mysql://ps-dev-mysqlserver02.mysql.database.azure.com:3306/petclinic}
# spring.datasource.username=${MYSQL_USER:lasteagle3}
# spring.datasource.password=${MYSQL_PASS:D0NR@hul048}
# # SQL is written to be idempotent so this is safe
# spring.sql.init.mode=always

# database init, supports mysql too

# Below configuration is made to work with Local Docker setup with Mysql and spring app as individual containers 
# database=mysql
# spring.datasource.url=${MYSQL_URL:jdbc:mysql://mysql-db:3306/petclinic?useSSL=false&allowPublicKeyRetrieval=true}
# spring.datasource.username=${MYSQL_USER:petclinic}
# spring.datasource.password=${MYSQL_PASS:petclinic}
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.defer-datasource-initialization=true

# # SQL is written to be idempotent so this is safe
# spring.sql.init.mode=always
# spring.sql.init.continue-on-error=true

# Below Configuration is updated to use Kubernetes Environment variables for MYsql information 
spring.datasource.url=jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT:3306}/${MYSQL_DATABASE}
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=none